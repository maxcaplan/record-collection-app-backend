FROM mongo:latest

EXPOSE 27017

WORKDIR /db

ENV MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
ENV MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}

ADD . .

CMD [ "--auth", "-f", "./mongod.conf" ]